import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
};

const CustomerPage = () => {
  return (
    <main>
      <h1>Customer Page</h1>
    </main>
  );
};

export default CustomerPage;
